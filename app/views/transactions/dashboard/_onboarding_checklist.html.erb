<% return unless show_onboarding_checklist? %>

<div class="bg-blue-50 border border-blue-200 rounded-xl p-4 mb-6 onboarding-checklist">
  <h2 class="text-sm font-semibold text-blue-800 mb-3">
    <%= t(".title") %>
  </h2>

  <ul class="space-y-2 text-sm text-blue-900">
    <li class="flex items-center gap-2">
      <%= current_user.accounts.any? ? "✅" : "🔹" %>
      <% if current_user.accounts.any? %>
        <%= t(".account_created") %>
      <% else %>
        <%= link_to t(".create_account"), new_account_path, data: { turbo_frame: "modal" }, class: "underline hover:text-blue-600" %>
      <% end %>
    </li>

    <li class="flex items-center gap-2">
      <%= current_user.transactions.any? ? "✅" : "🔹" %>
      <% if current_user.transactions.any? %>
        <%= t(".transaction_created") %>
      <% else %>
        <%= link_to t(".create_transaction"), new_transaction_path, data: { turbo_frame: "modal" }, class: "underline hover:text-blue-600" %>
      <% end %>
    </li>

    <li class="flex items-center gap-2">
      <%= current_user.tags.any? ? "✅" : "🔹" %>
      <% if current_user.tags.any? %>
        <%= t(".tags_created") %>
      <% else %>
        <span class="text-blue-700"><%= t(".add_tags_hint") %></span>
      <% end %>
    </li>

    <li class="flex items-center gap-2">
      <%= current_user.budgets.any? ? "✅" : "🔹" %>
      <% if current_user.budgets.any? %>
        <%= t(".budget_created") %>
      <% else %>
        <%= link_to t(".create_budget"), new_budget_path, data: { turbo_frame: "modal" }, class: "underline hover:text-blue-600" %>
      <% end %>
    </li>
  </ul>
</div>
